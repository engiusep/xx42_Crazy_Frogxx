#include "ClapTrap.hpp"




int main()
{
    ClapTrap trap("Hugo");
    ClapTrap a("Stev");

    trap.attack(a.getName());
    trap.attack(a.getName());
    trap.attack(a.getName());
    trap.attack(a.getName());
    trap.attack(a.getName());
    trap.attack(a.getName());
    trap.attack(a.getName());
    trap.attack(a.getName());
    trap.attack(a.getName());
    trap.attack(a.getName());
    trap.attack(a.getName());
    trap.attack(a.getName());
    trap.attack(a.getName());
    trap.attack(a.getName());
    trap.attack(a.getName());


}